import React from "react"
import Header from '../components/header'

export default () => (
    <div style={{ color: `teal` }}>
        <Header headerText='About dezz nuts'/>
        <Header headerText='suck them' />
        <p>Such wow. Very React.</p>
    </div>
)